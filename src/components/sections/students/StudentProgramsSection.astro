<section
  class="student-programs section"
  aria-labelledby="student-programs-title"
>
  <div class="container student-programs__grid">
    <header>
      <p>Programs and development support</p>
      <h2 id="student-programs-title">
        A program should support a stage—not replace the journey.
      </h2>
    </header>
    <div>
      <p>
        Structured programs may support different parts of employability
        development. Pathwisse will publish program names, formats, access
        details, and outcomes only after they are verified.
      </p>
      <a class="action-link" href="/programs">
        Explore Programs
      </a>
    </div>
  </div>
</section>

<style>
  .student-programs__grid {
    display: grid;
    grid-template-columns: 0.9fr 1.1fr;
    gap: clamp(3rem, 9vw, 9rem);
  }
  .student-programs header p {
    color: var(--color-primary);
    font-size: var(--text-small);
    font-weight: var(--font-weight-semibold);
  }
  .student-programs__grid > div p {
    color: var(--color-text-muted);
    font-size: var(--text-body-lg);
  }
  @media (max-width: 48rem) {
    .student-programs__grid {
      grid-template-columns: 1fr;
    }
  }
</style>
