<section class="connected section" aria-labelledby="connected-title">
  <div class="container connected__grid">
    <header>
      <p>How the stages connect</p>
      <h2 id="connected-title">A connected journey changes the question.</h2>
    </header>
    <div class="connected__comparison">
      <div>
        <p>Disconnected activity</p>
        <p class="connected__sequence">
          Course <span>→</span> Certificate <span>→</span> Forget <span>→</span>{" "}
          Another course
        </p>
      </div>
      <div class="connected__better">
        <p>Connected employability journey</p>
        <p class="connected__sequence">
          Destination <span>→</span> Development <span>→</span> Application{" "}
          <span>→</span> Demonstration <span>→</span> Preparation
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  .connected__grid {
    display: grid;
    grid-template-columns: 0.7fr 1.3fr;
    gap: clamp(3rem, 9vw, 9rem);
  }
  .connected header > p {
    color: var(--color-primary);
    font-size: var(--text-small);
    font-weight: var(--font-weight-semibold);
  }
  .connected__comparison {
    border-top: 1px solid var(--color-border-strong);
  }
  .connected__comparison > div {
    padding-block: var(--space-8);
    border-bottom: 1px solid var(--color-border);
  }
  .connected__comparison > div > p:first-child {
    color: var(--color-text-muted);
    font-size: var(--text-small);
  }
  .connected__sequence {
    margin: 0;
    font-size: var(--text-body-lg);
    font-weight: var(--font-weight-medium);
  }
  .connected__sequence span {
    margin-inline: 0.35rem;
    color: var(--color-text-muted);
  }
  .connected__better {
    color: var(--color-primary);
  }
  @media (max-width: 48rem) {
    .connected__grid {
      grid-template-columns: 1fr;
    }
    .connected__sequence span {
      display: block;
      margin: var(--space-2) 0;
    }
  }
</style>
