<section
  class="hiw-hero section section--spacious"
  aria-labelledby="page-title"
>
  <div class="container-wide hiw-hero__grid">
    <div>
      <p class="hiw-hero__eyebrow">The Pathwisse approach</p>
      <h1 id="page-title">Build employability as a journey, not an add-on.</h1>
    </div>
    <div class="hiw-hero__aside">
      <p>
        Pathwisse is being built around one progression: direction to
        opportunity.
      </p>
      <a class="action-link" href="#employability-model">
        See the complete model
      </a>
    </div>
  </div>
  <div class="container-wide hiw-hero__line" aria-hidden="true">
    <span></span>
  </div>
</section>

<style>
  .hiw-hero__grid {
    display: grid;
    grid-template-columns: 1.25fr 0.75fr;
    gap: clamp(3rem, 9vw, 9rem);
    align-items: end;
  }
  .hiw-hero__eyebrow {
    color: var(--color-primary);
    font-size: var(--text-caption);
    font-weight: var(--font-weight-semibold);
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }
  .hiw-hero h1 {
    max-width: 15ch;
    margin: var(--space-6) 0 0;
  }
  .hiw-hero__aside > p {
    color: var(--color-text-muted);
    font-size: var(--text-body-lg);
  }
  .hiw-hero__line {
    height: 3rem;
    margin-top: var(--space-12);
    border-top: 1px solid var(--color-border);
  }
  .hiw-hero__line span {
    display: block;
    width: 33%;
    height: 2px;
    background: var(--color-primary);
  }
  @media (max-width: 48rem) {
    .hiw-hero__grid {
      grid-template-columns: 1fr;
      gap: var(--space-8);
    }
    .hiw-hero__line {
      height: 3rem;
    }
  }
</style>
