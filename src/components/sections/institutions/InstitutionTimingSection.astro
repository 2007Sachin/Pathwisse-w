<section class="timing section section--dark" aria-labelledby="timing-title">
  <div class="container timing__grid">
    <p>The timing problem</p>
    <div>
      <h2 id="timing-title">
        The student journey starts long before the placement calendar.
      </h2>
      <p>
        Students may complete courses, workshops, projects, training, and
        placement preparation at different moments. When those experiences
        remain separate, the full development story can be difficult to
        understand by the time placement season arrives.
      </p>
      <p class="timing__boundary">
        This is a strategic problem framing, not a measured claim about every
        institution.
      </p>
    </div>
  </div>
</section>

<style>
  .timing__grid {
    display: grid;
    grid-template-columns: 0.45fr 1.55fr;
    gap: var(--space-16);
  }
  .timing__grid > p {
    color: #79a8ff;
    font-size: var(--text-small);
    font-weight: var(--font-weight-semibold);
  }
  .timing__grid div > p {
    max-width: 48rem;
    color: var(--color-text-muted-on-dark);
    font-size: var(--text-body-lg);
  }
  .timing__boundary {
    margin-top: var(--space-8);
    padding-top: var(--space-5);
    border-top: 1px solid rgb(255 255 255 / 0.22);
    font-size: var(--text-small) !important;
  }
  @media (max-width: 48rem) {
    .timing__grid {
      grid-template-columns: 1fr;
      gap: var(--space-5);
    }
  }
</style>
