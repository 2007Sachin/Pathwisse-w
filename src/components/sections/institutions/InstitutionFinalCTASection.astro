<section
  class="institution-final section section--subtle"
  aria-labelledby="institution-final-title"
>
  <div class="container institution-final__grid">
    <div>
      <p>Continue the institutional conversation</p>
      <h2 id="institution-final-title">
        Build employability into the journey, not the calendar.
      </h2>
    </div>
    <div>
      <p>
        Start with your context and the questions that still need verified
        answers.
      </p>
      <div class="cluster">
        <a class="button button--primary" href="/contact">
          Request a Demo
        </a>
        <a class="action-link" href="/how-it-works">
          See How It Works
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  .institution-final__grid {
    display: grid;
    grid-template-columns: 1fr 0.7fr;
    gap: clamp(3rem, 9vw, 9rem);
    align-items: end;
  }
  .institution-final__grid > div:first-child p {
    color: var(--color-primary);
    font-size: var(--text-small);
    font-weight: var(--font-weight-semibold);
  }
  .institution-final__grid > div:last-child > p {
    color: var(--color-text-muted);
    font-size: var(--text-body-lg);
  }
  @media (max-width: 48rem) {
    .institution-final__grid {
      grid-template-columns: 1fr;
    }
  }
</style>
