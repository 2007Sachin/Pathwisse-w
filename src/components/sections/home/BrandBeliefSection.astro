<section
  id="brand-belief"
  class="belief section section--spacious"
  aria-labelledby="belief-title"
>
  <div class="container belief__content">
    <p>What we believe</p>
    <h2 id="belief-title">
      Education should not end with <span>“I completed it.”</span>
    </h2>
    <p class="belief__answer">
      It should move toward: <strong>“I can show you what I can do.”</strong>
    </p>
  </div>
</section>

<style>
  .belief__content > p:first-child {
    color: var(--color-primary);
    font-size: var(--text-small);
    font-weight: var(--font-weight-semibold);
  }
  .belief h2 {
    max-width: 14ch;
    margin-block: var(--space-8);
    font-size: var(--text-statement);
    line-height: 1;
  }
  .belief h2 span {
    color: var(--color-text-muted);
  }
  .belief__answer {
    max-width: 35rem;
    margin: 0 0 0 auto;
    color: var(--color-text-muted);
    font-size: var(--text-body-lg);
  }
  .belief__answer strong {
    color: var(--color-primary);
    font-weight: var(--font-weight-medium);
  }
</style>
