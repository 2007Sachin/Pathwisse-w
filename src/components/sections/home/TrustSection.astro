<section
  id="trust"
  class="trust section section--subtle"
  aria-labelledby="trust-title"
>
  <div class="container trust__grid">
    <header>
      <p>Trust through clarity</p>
      <h2 id="trust-title">Credibility should be earned, not implied.</h2>
    </header>
    <div class="trust__statement">
      <p>
        Built by Shaquantum Labs Private Limited. We separate product intent,
        illustrative journeys, and verified proof.
      </p>
      <p>
        Pathwisse does not promise placements, salaries, rankings, or guaranteed
        outcomes.
      </p>
    </div>
    <dl class="trust__method">
      <div>
        <dt>Method</dt>
        <dd>Direction → capability → evidence → opportunity.</dd>
      </div>
      <div>
        <dt>Product</dt>
        <dd>Only approved product experiences should be shown as real.</dd>
      </div>
      <div>
        <dt>Claims</dt>
        <dd>
          Strong claims require product or business evidence before publication.
        </dd>
      </div>
      <div>
        <dt>Responsibility</dt>
        <dd>
          Privacy and data-use details must be clear before collection begins.
        </dd>
      </div>
    </dl>
  </div>
</section>

<style>
  .trust__grid {
    display: grid;
    grid-template-columns: 0.8fr 1.2fr;
    gap: var(--space-12) var(--space-16);
  }
  .trust header p {
    color: var(--color-primary);
    font-size: var(--text-small);
    font-weight: var(--font-weight-semibold);
  }
  .trust__statement {
    color: var(--color-text-muted);
    font-size: var(--text-body-lg);
  }
  .trust__statement p:last-child {
    color: var(--color-ink);
    font-weight: var(--font-weight-medium);
  }
  .trust__method {
    grid-column: 1 / -1;
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    margin: var(--space-8) 0 0;
    border-top: var(--border-width) solid var(--color-border-strong);
  }
  .trust__method div {
    display: grid;
    grid-template-columns: 0.35fr 0.65fr;
    gap: var(--space-5);
    padding: var(--space-6) var(--space-6) var(--space-6) 0;
    border-bottom: var(--border-width) solid var(--color-border);
  }
  .trust__method dt {
    color: var(--color-primary);
    font-size: var(--text-small);
    font-weight: var(--font-weight-semibold);
  }
  .trust__method dd {
    margin: 0;
    color: var(--color-text-muted);
  }
  @media (max-width: 48rem) {
    .trust__grid,
    .trust__method {
      grid-template-columns: 1fr;
    }
    .trust__method div {
      grid-template-columns: 1fr;
    }
  }
</style>
