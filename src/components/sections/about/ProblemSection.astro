<section
  class="about-problem section section--subtle"
  aria-labelledby="about-problem-title"
>
  <div class="container about-problem__grid">
    <header>
      <p>The problem we saw</p>
      <h2 id="about-problem-title">
        The work can accumulate without becoming one clear story.
      </h2>
    </header>
    <div>
      <p>
        Students may move through classes, courses, certificates, workshops,
        assignments, projects, and placement preparation. Each experience can
        matter on its own.
      </p>
      <p>
        The difficulty appears when those experiences remain disconnected and a
        student is eventually asked to explain what they can do, where they used
        it, and what they learned.
      </p>
    </div>
  </div>
</section>

<style>
  .about-problem__grid {
    display: grid;
    grid-template-columns: 0.8fr 1.2fr;
    gap: clamp(3rem, 9vw, 9rem);
  }
  .about-problem header p {
    color: var(--color-primary);
    font-size: var(--text-small);
    font-weight: var(--font-weight-semibold);
  }
  .about-problem__grid > div p {
    color: var(--color-text-muted);
    font-size: var(--text-body-lg);
  }
  @media (max-width: 48rem) {
    .about-problem__grid {
      grid-template-columns: 1fr;
    }
  }
</style>
