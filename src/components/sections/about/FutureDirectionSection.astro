<section
  class="future-direction section section--subtle"
  aria-labelledby="future-direction-title"
>
  <div class="container future-direction__grid">
    <p>Where we are heading</p>
    <div>
      <h2 id="future-direction-title">
        Toward an earlier, more connected way to think about employability.
      </h2>
      <p>
        We are building toward a journey in which students can connect
        direction, development, application, and preparation—and institutions
        can describe how those ideas fit together in their guidance approach.
      </p>
      <p>
        Stronger product, program, and institutional details belong here after
        they have approved evidence.
      </p>
    </div>
  </div>
</section>

<style>
  .future-direction__grid {
    display: grid;
    grid-template-columns: 0.45fr 1.55fr;
    gap: var(--space-16);
  }
  .future-direction__grid > p {
    color: var(--color-primary);
    font-size: var(--text-small);
    font-weight: var(--font-weight-semibold);
  }
  .future-direction__grid div > p {
    max-width: 49rem;
    color: var(--color-text-muted);
    font-size: var(--text-body-lg);
  }
  .future-direction__grid div > p:last-child {
    margin-top: var(--space-8);
    padding-top: var(--space-5);
    border-top: 1px solid var(--color-border-strong);
    font-size: var(--text-small);
  }
  @media (max-width: 48rem) {
    .future-direction__grid {
      grid-template-columns: 1fr;
      gap: var(--space-5);
    }
  }
</style>
