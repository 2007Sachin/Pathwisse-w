<section
  class="last-mile section section--dark"
  aria-labelledby="last-mile-title"
>
  <div class="container last-mile__grid">
    <p>The last mile</p>
    <div>
      <h2 id="last-mile-title">
        The transition from learning to being able to explain capability.
      </h2>
      <p>
        “The last mile of education” is Pathwisse shorthand for the space
        between learning something and communicating its relevance in a hiring
        context.
      </p>
      <p>
        It does not mean education matters only for employment, that
        universities are failing, or that Pathwisse can guarantee a job. It
        names one transition that deserves greater connection and intention.
      </p>
    </div>
  </div>
</section>

<style>
  .last-mile__grid {
    display: grid;
    grid-template-columns: 0.45fr 1.55fr;
    gap: var(--space-16);
  }
  .last-mile__grid > p {
    color: #79a8ff;
    font-size: var(--text-small);
    font-weight: var(--font-weight-semibold);
  }
  .last-mile__grid div > p {
    max-width: 49rem;
    color: var(--color-text-muted-on-dark);
    font-size: var(--text-body-lg);
  }
  @media (max-width: 48rem) {
    .last-mile__grid {
      grid-template-columns: 1fr;
      gap: var(--space-5);
    }
  }
</style>
