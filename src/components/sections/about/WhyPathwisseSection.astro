<section class="why-pathwisse section" aria-labelledby="why-pathwisse-title">
  <div class="container why-pathwisse__grid">
    <header>
      <p>Why Pathwisse exists</p>
      <h2 id="why-pathwisse-title">
        Bring employability into one coherent journey.
      </h2>
    </header>
    <div>
      <p>
        The aim: a journey that is easier to understand before students must
        explain what they can do.
      </p>
    </div>
  </div>
</section>

<style>
  .why-pathwisse__grid {
    display: grid;
    grid-template-columns: 0.85fr 1.15fr;
    gap: clamp(3rem, 9vw, 9rem);
  }
  .why-pathwisse header p {
    color: var(--color-primary);
    font-size: var(--text-small);
    font-weight: var(--font-weight-semibold);
  }
  .why-pathwisse__grid > div p {
    color: var(--color-text-muted);
    font-size: var(--text-body-lg);
  }
  .why-pathwisse__grid > div p + p {
    margin-top: var(--space-8);
    padding-top: var(--space-5);
    border-top: 1px solid var(--color-border);
    font-size: var(--text-small);
  }
  @media (max-width: 48rem) {
    .why-pathwisse__grid {
      grid-template-columns: 1fr;
    }
  }
</style>
