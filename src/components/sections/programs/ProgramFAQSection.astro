---
import { programFaqs } from "../../../content/pages/programs";
---

<section
  class="program-faq section section--subtle"
  aria-labelledby="program-faq-title"
>
  <div class="container program-faq__grid">
    <header>
      <p>Program questions</p>
      <h2 id="program-faq-title">
        What can be answered before a catalogue is approved.
      </h2>
    </header>
    <div>
      {programFaqs.map((item) => (
        <details>
          <summary>{item.question}</summary>
          <p>{item.answer}</p>
        </details>
      ))}
    </div>
  </div>
</section>

<style>
  .program-faq__grid {
    display: grid;
    grid-template-columns: 0.8fr 1.2fr;
    gap: clamp(3rem, 9vw, 9rem);
  }
  .program-faq header p {
    color: var(--color-primary);
    font-size: var(--text-small);
    font-weight: var(--font-weight-semibold);
  }
  .program-faq details {
    border-top: 1px solid var(--color-border-strong);
  }
  .program-faq details:last-child {
    border-bottom: 1px solid var(--color-border-strong);
  }
  .program-faq summary {
    min-height: 3rem;
    padding: var(--space-6) var(--space-10) var(--space-6) 0;
    color: var(--color-ink);
    font-size: var(--text-body-lg);
    font-weight: var(--font-weight-medium);
    cursor: pointer;
  }
  .program-faq details p {
    max-width: 42rem;
    padding: 0 var(--space-10) var(--space-6) 0;
    color: var(--color-text-muted);
  }
  @media (max-width: 48rem) {
    .program-faq__grid {
      grid-template-columns: 1fr;
    }
  }
</style>
